#include <iostream>
#include <vector>
using namespace std;

typedef long long int ll;
int main()
{
    ll n;
    int min = 0;
    cin >> n;
    vector<ll> v(n);
    for (auto& it : v)
        cin >> it;
    idx = 2; // Удаляем элемент с 3 элементом
    vec.erase(vec.begin() + idx);
    return 0;
}
